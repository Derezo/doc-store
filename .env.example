NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://docstore:docstore_dev@localhost:5432/docstore
JWT_SECRET=dev-secret-change-me-in-production-please-32chars
DATA_DIR=./data/vaults
BASE_URL=http://localhost:4000
WEB_URL=http://localhost:3000
SMTP_HOST=
SMTP_FROM=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# ── Production Configuration ────────────────────────────────────────
# NEXT_PUBLIC_API_URL is baked into the Next.js bundle at build time.
# Set it in .env.production BEFORE running `npm run build`.
#
# Recommended .env.production values:
#   NODE_ENV=production
#   PORT=4000
#   DATABASE_URL=postgresql://docstore:STRONG_PASSWORD@localhost:5432/docstore_production
#   JWT_SECRET=<64-char-random-hex: openssl rand -hex 32>
#   DATA_DIR=/var/data/doc-store/vaults
#   BASE_URL=https://vault.mittonvillage.com
#   WEB_URL=https://vault.mittonvillage.com
#   NEXT_PUBLIC_API_URL=https://vault.mittonvillage.com
